package com.example.examm4.repository;

import com.example.examm4.model.ClassType;
import org.springframework.data.jpa.repository.JpaRepository;

public interface IClassTypeRepository extends JpaRepository<ClassType, Integer> {
}
